<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SmartOfficeClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="FullScreenDialogTheme" parent="Theme.MaterialComponents.DialogWhenLarge">
        <item name="windowNoTitle">true</item>
        <item name="backgroundTint">@color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
        <item name="android:windowAnimationStyle">@style/Animation.AppCompat.Dialog</item>
    </style>
    <style name="DatePicker">
        <item name="android:background">@color/background_date_dark</item>
    </style>
    <!-- Для темной темы -->
    <style name="DarkTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:textColorHint">@color/white</item>
        <item name="boxStrokeColor">@color/white</item>
        <!-- Добавьте другие атрибуты, если необходимо -->
    </style>

    <style name="BeautifulButtonStyle">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:background">@drawable/custom_button_background</item>
        <item name="android:padding">8dp</item>
        <!-- Добавьте другие атрибуты стиля, которые вам необходимы -->
    </style>
</resources>